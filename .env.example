# ============================================================================
# ENVIRONMENT VARIABLES - Team Calendar App
# ============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git — it contains secrets.
# ============================================================================

# --- Frontend (Vercel) ---
# Get these from your Supabase project: Settings > API
REACT_APP_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Backend API URL (Railway deployment or local)
REACT_APP_API_URL=https://your-railway-app.up.railway.app
# Must match the API_KEY set on Railway
REACT_APP_API_KEY=your-api-key-here

# Super admin PIN for Quick Actions (defaults to 1426 if not set)
REACT_APP_ADMIN_PIN=1426

# --- Backend (Railway) ---
# Same Supabase project, but use the SERVICE ROLE key (not anon)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# API key — must match REACT_APP_API_KEY above
API_KEY=your-api-key-here

# Railway sets PORT automatically — don't set it locally
# PORT=5000

# Optional: restrict CORS origins (comma-separated)
# Defaults to https://teamcalendar.mygymtools.com
# CORS_ORIGINS=https://teamcalendar.mygymtools.com,http://localhost:3000
